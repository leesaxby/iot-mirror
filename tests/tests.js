//import Foo from '../app/src/js/app.js';

describe('ES6 Foo', function () {

    //  beforeEach(function(done) {
    //      setTimeout(function() {
    //          done();
    //      }, 1000);
    //  });

/*
    let foo;

    beforeEach(()=>{
        foo = new Foo();
    });

    it('should return Do Something when calling doSomething', ()=>{
      expect(foo.doSomething()).toEqual('Do Something');
    });
*/
  //  it('should return Do Something when calling doSomething', function( done ) {
  //      expect(1).toEqual(1);
  //      done();
  //  });
});
